# Alby Hub ? Connections ? New Connection ? copy full string
NWC_URL=nostr+walletconnect://paste_here

# Optional: only if using getalby.com cloud API instead of Hub
# ALBY_API_TOKEN=your_token_here

PORT=3000
